package com.example.emyntra;

import com.journeyapps.barcodescanner.CaptureActivity;

// This class extends the library's CaptureActivity
// It allows us to lock the orientation to Portrait in the Manifest
public class CaptureAct extends CaptureActivity {
    // No code needed here, the magic happens in AndroidManifest.xml
}
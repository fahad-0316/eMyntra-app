<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- This file now only contains your custom component styles. -->
    <!-- The main 'Theme.EMyntra' is now in your 'themes.xml' file. -->

    <!-- Style for our custom EditText -->
    <style name="AppTheme.EditText" parent="Widget.AppCompat.EditText">
        <item name="android:background">@drawable/edittext_background</item>
        <item name="android:padding">16dp</item>
        <item name="android:textColor">@color/colorTextPrimary</item>
        <item name="android:textColorHint">#888888</item>
    </style>

    <!-- Style for our custom Button -->
    <style name="AppTheme.Button" parent="Widget.AppCompat.Button">
        <!-- THIS IS THE NEW LINE THAT FIXES THE COLOR -->
        <item name="backgroundTint">@null</item>

        <item name="android:background">@drawable/button_background</item>
        <item name="android:textColor">@color/colorTextOnPrimary</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:paddingTop">14dp</item>
        <item name="android:paddingBottom">14dp</item>
    </style>

</resources>